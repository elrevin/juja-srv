{% extends '@themelayouts/inner.twig' %}
{% block content %}
    <h1><span class="text-muted">{{ item.date|date("d.m.Y") }}</span> {{ data.title }}</h1>
    <div class="row">
        <div class="col-xs-12">
            {{ data.text }}
        </div>
    </div>
{% endblock %}